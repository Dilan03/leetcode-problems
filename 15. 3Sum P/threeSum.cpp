#include <iostream>
#include <vector>
#include <assert.h>
using namespace std;

class Solution {
public:
   vector<vector<int>> threeSum(vector<int>& nums) {
        
    }
};

int main() {
   Solution solution;

   vector<vector<int>> nums = {{-1, -1, 2}, { -1, 0, 1 }};

   for (int i = 0; i < nums.size(); i++)
   {
      for (int j = 0; j < nums[0].size(); j++)
      {
         cout << nums[i][j];
      }
   }
}